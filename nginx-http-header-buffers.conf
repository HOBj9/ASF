# Include in nginx: add to http block or copy to /etc/nginx/conf.d/large-headers.conf
# Fixes HTTP 431 (Request Header Fields Too Large) when using large cookies (e.g. NextAuth JWT)
client_header_buffer_size 32k;
large_client_header_buffers 8 64k;
